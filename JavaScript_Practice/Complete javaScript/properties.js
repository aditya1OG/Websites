//👍😄😏 Properties : innerHtml, outerHtml   👍😄😏//

//😄 InnerHtml :(Valid for element nodes only) the innerhtml property allows to get the html inside the element as a string.
// innerHtml is valid only for element nodes. for other node types we can use nodevalue or data.

//😄 OuterHtml : the outerHtml property contains the full HTML. innerHTML + the element itself.

//😄 Text content : provides access to the text inside the element. only text minus all the tags
//  console.log(document.body.textContent)

//😄 The hidden property : the hidden attribute and the DOM property specifies wheather the element is visible or not. 